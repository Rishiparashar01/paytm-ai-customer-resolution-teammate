# Frontend environment
# Copy to frontend/.env.local to override. NEVER commit real env files.

# Base URL of the backend API (no trailing slash).
# Dev default: http://localhost:4000
# Production: set to your deployed backend origin, e.g. https://api.example.com
VITE_API_BASE_URL=http://localhost:4000
